#include <stdio.h>

struct node
{
    int data = NULL;
    struct node * right;
    struct node * left;
};
typedef struct node node;

int main (void)
{
    node * root = malloc(sizeof(node));
    while (1)
    {
        int i;
        node * temp = 
        printf("Enter Number: ");
        scanf("%d", &i);
        insertion = malloc(sizeof(node));
        if(insertion.data > )
        {

        }
    }
    return 1;
}